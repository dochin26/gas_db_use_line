# 要約

スプレッドシートの中にあるワードを検索し、該当すれば特定の文字列を出力するGASアプリケーションです。
入出力はLineで行います。

# 使用例
ここでは、予め漢和辞典の漢字と記載ページ数をスプレッドシートに入力します。
調べたい漢字をLineで送信し、GASは該当する漢字を検索します。
該当すれば、対応したページ数を出力します。
該当がなければ、答えが見つからない・・・と出力します。
